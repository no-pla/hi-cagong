import { IntroItem } from './IntroItem';

const MapContainer = () => {
  return (
    <>
      <div
        id="myMap"
        style={{
          width: '100%',
          height: '100%',
        }}
      />
      <IntroItem />
    </>
  );
};

export default MapContainer;
