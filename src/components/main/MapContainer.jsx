import { IntroItem } from './IntroItem';

const MapContainer = ({ placeItem }) => {
  return (
    <>
      <div
        id="myMap"
        style={{
          width: '100%',
          height: '100%',
        }}
      />
    </>
  );
};

export default MapContainer;
